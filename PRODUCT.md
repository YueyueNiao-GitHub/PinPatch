# Product

## Register

product

## Users

主要用户是参与 AI 前端协作的产品、设计、前端、后端与全栈人员。他们在本地预览页面中发现样式、文案、布局或状态问题，希望不必理解完整 DOM 和工程结构，也能准确指出目标元素并把修改意图交给智能体。

## Product Purpose

产品通过页面点选、标注、评论线程和结构化上下文输出，连接“用户在浏览器里看到的问题”与“智能体修改代码所需的上下文”。成功意味着用户能用更少的描述和返工，在一到两轮内完成局部页面修改，同时避免无关重构。

## Brand Personality

克制、准确、工具化。界面应让用户感觉轻量、可信、随时可退出，视觉表达服务于当前任务，不抢夺页面内容的注意力。

## Anti-references

- 不做完整低代码编辑器或设计器，不堆叠复杂属性面板。
- 不伪装成 IDE，不替代代码评审或开发者的质量责任。
- 不采用泛化 SaaS 仪表盘式装饰、过度动画或高饱和非状态色。
- 不让反馈工具遮挡、干扰或永久改变被标注页面的正常交互。

## Design Principles

- 指向准确：任何操作都应帮助用户确认“我选中了哪里”。
- 上下文可检查：交给智能体之前，用户能理解和确认将被传递的信息。
- 任务不中断：常用动作就近出现，工具栏保持轻量，标注模式可快速进入和退出。
- 状态清晰：待处理、已解决等人工验收状态具有一致且可识别的反馈。
- 本地优先：默认不上传页面、DOM、源码路径或用户反馈。

## Accessibility & Inclusion

交互控件需要可键盘操作、具备明确的可访问名称与可见焦点；颜色不能作为唯一状态线索；触控目标保持至少 44×44 像素；响应式布局应保证窄屏下的核心操作可用；状态动效应简短，并尊重减少动态效果偏好。
